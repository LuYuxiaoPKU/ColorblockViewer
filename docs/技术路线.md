# 技术路线综述

> 本文档是 v1.0 发布前对**当前 README 的存档**，记录项目的完整技术路线：
> 架构决策、1:1 语义复刻方法论、里程碑演进、验证策略与工程约束。
> 项目简要介绍见根目录 [README.md](../README.md)。

---

## 1. 项目定位与目标

纯前端静态网站：在浏览器内预览 Minecraft Fabric 模组 **AnotherColorBlock**
的 `/particleex` 粒子效果，以及**原版** `/particle` 命令（粒子类型表/贴图按
1.21.11 / 26.2 分区，可切换）。用户粘贴命令（或使用结构化表单），浏览器内
3D 渲染粒子生成与逐 tick 动画——无需安装游戏/模组即可调试粒子表达式。

核心目标按优先级：

1. **语义正确性（最高）**：对模组 Java 源码 1:1 语义移植——表达式求值结果、
   粒子生命周期行为、**错误消息文本**都逐字一致；用 golden 测试锁行为。
2. **命令覆盖完整**：模组 10 种 `/particleex` 命令 + `group remove|change` +
   `clearparticle`，以及原版 `/particle` 命令树（逐字核对 `ParticleCommand`）。
3. **可调试性**：表达式实时校验（英文原文 + 中文提示）、命令双向同步表单、
   播放控制（单步/倍速/重置）。
4. **零部署成本**：无后端、无构建产物依赖，GitHub Pages 静态托管。

## 2. 技术选型与理由

| 选型 | 理由 |
|---|---|
| React 19 + TypeScript | 结构化表单/面板的组件化；TS 严格模式保证移植层类型安全 |
| Three.js 0.185 + 自定义点云 shader | 粒子是大量小四边形，`THREE.Points` + 自定义 attribute（color/size/uv）最简；加色混合软光晕贴近游戏观感 |
| Vite 8 | 构建快；`base: '/ColorblockViewer/'` 与仓库名一致以适配 GitHub Pages 子路径 |
| Vitest | 与 Vite 同栈；`environment: 'node'` 下引擎/解析/仿真全可测，UI 用 happy-dom |
| `useSyncExternalStore` 轻量外部 store | 命令列表/设置是跨组件共享的"真源"，无需引入状态库 |
| 无后端 | 所有逻辑（表达式引擎、仿真、渲染）纯前端可跑，静态站点即可托管 |

## 3. 1:1 语义移植方法论（本项目最重要的工程决策）

**原则**：预览工具的价值在于"所见即游戏所得"，因此对模组 Java 源码做逐字
语义核对而非行为近似。落地为三层保障：

- **golden 测试**：从 Java 原版跑出的表达式求值结果/报错文本固化为测试向量，
  TS 引擎必须逐字复现（Int32 除法/取模、常量折叠、矩阵函数、函数重载、
  错误消息措辞）。
- **缺陷也复刻**：Java 版的既有缺陷按原样保留而非"顺手修掉"，包括
  - customMove 的 pre/stop 回滚逻辑；
  - `age` 恒 0 的缺陷（导致默认寿命走近似值路径）；
  - 组索引惰性清理；
  - `group change` 遇 do-while 死粒子时 cexe==null 的 NPE → 命令中止（补测锁定）。
- **跨版本一致性验证**：对模组 1.21 / 1.21.11 / 26.1 / main 各分支做源码
  diff，确认命令树、参数类型、表达式引擎完全一致（差异仅在权限 API 与
  yarn 改名等渲染侧文件）→ 结论：1:1 移植（基准 1.21.11）对模组支持的
  全部 MC 版本（1.21.1–26.2）语义有效。

**表达式引擎结构**（`src/engine/`）：lexer → parser → AST → compiler
（闭包编译后端，表达式编译为 JS 闭包）→ runtime + mathfuncs/matrix/struct。
性能门槛：N=20000 sync 31 样本中位 < 15ms（CI runner 噪声抗抖动，见 commit
bbf9f18）；字节码保险丝触发条件 >25ms/tick。

**已知缺陷保留说明**：解析器对深嵌套括号表达式会挂死（递归无深度限制），
Java 原版同样挂死——按 1:1 原则保留；bench/测试表达式必须用浅层平衡树。

## 4. 架构分层

```
src/
├── engine/    表达式引擎（1:1 移植）：lexer/parser/ast/compiler/runtime/
│              mathfuncs/matrix/struct —— 纯函数，零 DOM 依赖，golden 测试覆盖
├── command/   命令解析：tokens/lexer→parser/schema（brigadier 树逐字核对）/
│              coords（~ 基准坐标）/serialize（结构化数组↔命令文本）
├── sim/       仿真引擎：engine（tick 驱动粒子生命周期）/groups（组管理）/
│              spawn（生成器）/rng（固定种子 PRNG）/structFill
├── render/    Three.js 渲染：scene（相机/网格/装配）/points（点云 shader +
│              贴图图集）/sync（快照→BufferAttribute）/particleData
│              （双版本粒子类型表与贴图映射）
├── store/     appState：useSyncExternalStore 外部 store（commands/设置/HUD/Toast）
└── ui/        面板：CommandPane + CommandForm（双向同步）+ ExprField
               （防抖编译校验 + 语法高亮）+ Viewport + SettingsDrawer + StatusToast
```

数据流单向：**文本/表单 → 结构化命令数组（唯一真源）→ 仿真引擎（20 TPS tick）
→ 快照 → BufferAttribute → GPU**。表单与命令文本双向同步，改任一侧都归一到
真源再渲染另一侧。

### 4.1 渲染管线要点

- 点云 shader：顶点 `gl_PointSize = size * uSizeMul * (uScale / -mvPosition.z)`，
  `uScale = h*0.5*min(dpr,2)/tan(fov/2)` 按视口高实时换算（视口 resize 时重算）；
  自定义 attribute 仅 `color`(vec4)/`size`(float)——**position/normal/uv 由
  three 非 raw ShaderMaterial 前缀声明，源码内不得重声明**（重声明 uv 曾致
  顶点着色器 redefinition 编译失败、全浏览器粒子黑屏，已加回归单测锁定）。
- 贴图：官方客户端 jar 提取的粒子图集（`npm run assets <版本>`），12 列 atlas；
  多帧类型走**原版 age-progress 选帧**（帧号 = floor(age·(N-1)/lifetime)，
  随粒子年龄线性推进、全寿命只播一遍不循环）+ 后 50% 寿命线性淡出（死亡瞬间
  alpha=0.5 而非 0）——1.21.1 反编译 `AnimatedParticle.tick` 逐字核对，
  见 §10 证据清单；`block`/`dust`/`item` 等按方块纹理实时渲染的类型
  与未知名回退软发光圆点，按类型微调 size/alpha/色相（TWEAKS 表）。
- 性能：sync 写 BufferAttribute 走快照合并；20 TPS 逻辑由墙钟累加器驱动，
  与 60Hz 渲染解耦。

### 4.2 原版 `/particle` 的双版本数据分区

粒子类型注册表与贴图按 **1.21.11（116 类型 / 251 贴图）** 与 **26.2（126 类型 /
285 贴图）** 分区，来源为 Mojang 官方客户端 jar（sha1 校验；注册表经字节码
提取，混淆版 ≤1.21.9 走 javap 标记扫描；data-driven 粒子 JSON 定义帧表）。
设置面板切换版本后，表单下拉与渲染图集同步切换，贴图按版本独立缓存。

## 5. 里程碑演进（git 历史）

| 里程碑 | commit | 内容 |
|---|---|---|
| M0 | a852d20 | Vite + React + TS 脚手架（base=/ColorblockViewer/，Vitest 跑通） |
| M1 | 4a8f2c2 | 表达式引擎 1:1 语义移植（闭包编译后端）+ golden 校验 + 性能门槛 |
| M2 | 1776d9b | 命令解析（10 粒子命令 + group remove/change + clearparticle） |
| M3 | 184e26b / 98dc776 | 仿真引擎 1:1 语义移植（生命周期 + 组操作 + tick 生成器）+ do-while 死粒子 NPE 补测 |
| M4 | e03b666 | Three.js 点云渲染（软光晕 shader + 场景装配 + 快照同步） |
| M5 | d6bf81d | UI 集成（命令面板 + 表单双向同步 + ExprField 校验 + 设置/播放/Toast） |
| M6 | 87288ad | 打磨与 GitHub Pages 部署准备 |
| 部署 | 9ae2328 / cc47833 | Pages 源切 GitHub Actions；CI 性能门槛按 runner 实际调整 |
| 贴图 | 9ef68a3 | 单引号表达式分词修复 + MC 26.2 官方粒子贴图 |
| 原版命令 | 7366950 | 原版 /particle 支持（MC 26.2） |
| 双版本 | 1087379 | 粒子类型表/贴图按 1.21.11 / 26.2 分区，设置面板切换 |

v1.0 前的关键修复链：`72f68ea`（「执行」先应用粘贴框内容，修"命令不生效"
stale-truth）、`f1fed62`（end_rod 尺寸 0.5→2.2，glitter 帧 8×8 仅 4–14 可见
像素原尺寸屏上不可见）、`251a1fc`（窄窗 ≤900px 单栏布局塌缩致视口行高 0 →
uScale=0 粒子全不可见，面板行 `minmax(0,45vh)` 修复）、`7e16b8a`（顶点着色器
重复声明 uv 致 WebGL 编译失败——线上全浏览器粒子黑屏的根因，自贴图功能引入，
删除重复声明 + 回归单测）。

## 6. 验证策略

- **695 个测试**：引擎 golden / 命令解析 / 仿真生命周期 / 渲染 / UI 集成，
  `npm test` 全量跑。
- **CI**（GitHub Actions，push main 触发）：`npm ci → npm test → tsc --noEmit
  → vite build → base 前缀断言 → Pages 部署`；任何一步失败不部署。
- **headless 浏览器探针**：chrome `--headless=new --dump-dom` 注入探针读
  canvas 尺寸/uScale/`renderer.info`，验证布局与渲染管线（注意：
  `--window-size` 的有效窗口不保证等于 flag 值；`renderer.info.render.*`
  只计 draw 尝试，program 无效时也计数，探针必须同时抓 `console.error`
  才能发现 shader 编译失败这类致命错误）。
- **性能门槛**：N=20000 sync 中位 < 15ms（31 样本抗抖动）；render 门槛按
  CI runner 实际 CPU 调整过（2ms → 5ms）。

## 7. 预览简化清单（与游戏内的已知差异）

- **贴图近似**：颜色乘法着色、统一加色混合；`block`/`dust`/`item` 等按方块
  纹理实时渲染的类型与未知名回退软发光圆点。帧动画与淡出已按原版
  age-progress 语义复刻（§4.1 / §10），不再是全局相位循环。
- **原版 `type{NBT}`**（如 `dust{Red:1f,…}`）不解析 NBT 载荷，按类型名近似
  （dust 用其帧表而非 NBT 指定颜色）。
- **原版命令寿命**：`kinematics.ts` 表内类型（26.2 约 50 个，见 §10）走原版
  随机/常量寿命公式（JDK 探针逐值对拍）；表外类型无逐类型数据 → 统一走
  「默认寿命」近似值。「原版运动学」设置关闭时全部走近似值。
- **原版 force/viewers 槽位**：与预览无关 → 解析层明确报错（而非静默忽略）。
- **运动学**：默认「原版运动学」开启，表内类型按 26.2 字节码逐字复刻摩擦/
  重力/运动模式（base 管道、portal 绝对位置式、reverse_portal 增量式）与
  campfire 出生初速；**表外类型仍走匀速直线**（dust 系、water_current_down
  等世界相关类型未做，见 §10 未建模清单）。该设置可关闭回退全类型匀速直线。
- **随机序列**：`normal` 高斯偏移与原版 delta/speed 用固定种子 PRNG；原版
  粒子随机寿命用独立 PRNG（seed+1），同一 `vanillaRand` 同时供应各类型的
  寿命公式随机量（campfire 另消费出生初速 float，顺序与构造器字节码一致）。
  均保证同一次预览可复现，不逐粒子对齐游戏内 Java `Random` 实例序列
  （原版逐粒子 `RandomSource.create()` 种子不可复现）。

## 8. 工程约束与环境备忘

- Git 身份：`LuYuxiaoPKU <LuYuxiaoPKU@users.noreply.github.com>`；commit 不加
  Co-Authored-By 行。
- CI runner 无 JDK；贴图提取脚本本地跑（本地 MC 安装 `MC_JAR` 环境变量优先，
  否则官方镜像；注册表提取需 javap）。产物已入库，日常构建不需要。
- MSYS/Git Bash 下 `vite build --base /` 类带前导斜杠参数需
  `MSYS_NO_PATHCONV=1` 防路径转换。
- 站点：https://luyuxiaopku.github.io/ColorblockViewer/（base 前缀
  `/ColorblockViewer/`，仓库名大小写必须一致）。

## 9. 后续方向（v1.0 后）

- 更多 MC 版本分区（新大版本发布时跑 `npm run assets <版本>` 提取入库）。
- `type{NBT}` 载荷解析（dust 颜色等）——目前按类型名近似。
- 表外类型的原版运动学参数（dust 系、firework 系等）——需逐类型字节码
  证据后入 `kinematics.ts` 表（见 §10 证据边界；2026-09-08 已覆盖
  世界数据无关的约 50 个类型）。
- 解析器深嵌套括号保护（需先与"1:1 保留缺陷"原则权衡）。

## 10. 原版粒子行为证据清单（end_rod / 帧动画 / 运动学）

预览中所有"原版行为"参数都有可追溯证据，未找到证据的行为不做：

| 行为 | 参数 | 证据 |
|---|---|---|
| 帧动画按年龄推进 | frame = floor(age·(N-1)/lifetime)，播一遍不循环 | 1.21.1 Yarn 反编译 `AnimatedParticle.tick → setSpriteForAge`（HY4 报告 A 节，2026-09 调研） |
| 后半程线性淡出 | age > maxAge/2 时 alpha = 1 − (age−maxAge/2)/maxAge，死亡瞬间 = 0.5 | 同上（`SimpleAnimatedParticle.tick`） |
| end_rod 颜色插值 | 每 tick 向 0xF2DEC9（rgb 242,222,201）靠拢 20% | 1.21.1 反编译 `EndRodParticle.setTargetColor(15916745)` |
| end_rod 随机寿命 | maxAge = 60 + random.nextInt(12) | 1.21.1 反编译 `EndRodParticle` 构造器 |
| end_rod 摩擦 | velocityMultiplier = 0.91/tick（位移之后乘） | 1.21.1 反编译 `EndRodParticle` 字段 |
| end_rod 重力 | gravityStrength = 0.0125 → Δvy = −0.04×0.0125 = −5e-4/tick²（位移之前加） | 同上 + `Particle.tick` 的 velocityY 更新顺序 |
| 26.2 约 50 类型运动学 | 逐类型 friction/gravityY/运动模式/postFriction/campfireRise，见 `src/sim/kinematics.ts` 表内注释 | 26.2 client.jar 全量 `javap -c -p`（`/tmp/partyphys/full.javap.txt`，12232 行）：`Particle.tick` 基类管道顺序 + 各 `$Provider`/构造器逐条核对 |
| 26.2 逐类型寿命公式 | 7 个公式族（div/divPlus/crit/ashScale/base/squid/portal/reversePortal/int/const），见 `NATIVE_LIFETIME` | 同上，构造器字节码；随机量用本地 JDK 21 探针逐值对拍（`rng.test.ts` nextFloat/nextInt goldens） |
| portal 漂移 | 绝对位置式 cubic easing（t=fdiv(age,life)；f1=(t·t)·2−t；e=1−f1；x=cx+vx·e；y+=1−t） | `PortalParticle.tick` 字节码逐浮点顺序复刻 |
| reverse_portal | 增量式 x += v·fround(age/lifetime)，寿命恒 60（60+2·(int)F 退化，1:1 保留） | `ReversePortalParticle.tick` 字节码 |
| campfire 出生初速 | age=0 时 vy += 500.0f/F，F 为寿命 I(50) 之后的下一个 nextFloat（float 除法） | `CampfireCosySmokeParticle` 构造器字节码；JDK 探针 `ProbeCampfire` 交错流实测 |

**未建模清单（有意不做，避免臆想）**：

- `Particle.tick` 的 `speedUpWhenYMotionIsBlocked`（y 位移被阻挡时 xd/zd×1.1）
  与 `onGround` 的额外地面摩擦（×0.7）——依赖碰撞/方块世界，预览无物理世界。
- 逐粒子 `RandomSource.create()` 种子——原版种子不可复现；预览用共享
  `vanillaRand`（seed+1）近似随机分布（end_rod 先例，见 `engine.ts` 注释）。
- campfire 每 tick 的 `F/5000±1` 逐轴随机漂移——分布量级极小且依赖逐粒子
  Random，仅保留出生初速。
- 依赖世界方块/流体状态的类型（`water_current_down` 的 isWater 检查等）。
- dust 系 / block / bubble / firework 系 / 未逐条核对的类型 → 保持匀速直线。

**本地交叉验证（不依赖二手报告）**：

- 被预览模组 jar（`AnotherColorBlock-1.21.11.jar`，noone89/Fabric，本地存于
  `~/Downloads/`，解包在 `/tmp/acb`）的 `ParticleMixin.customTick` 字节码：
  pre 记录 → `method_3070()`（原版 tick）**未取消** → stop 回滚 → customMove。
  模组的 mixin 不取消原版 `setAlpha`/`SpriteSet` → 上述原版动画/运动学在
  游戏内**全部生效**于 `/particleex` 生成的 end_rod 粒子（与游戏内"亮→灭、
  无闪烁"的观感一致）。
- **误归因更正**：早期调研报告中"ExParticle 的 mixin 取消 setAlpha → 不淡出"
  说的是 **hackermdch/ExParticle**（另一个 NeoForge 1.21.1 模组），与本项目
  预览的模组无关——本项目不需要"是否淡出"的 UI 开关。
- PRNG：`java.util.Random.nextInt(bound)` / `nextFloat()` 用本地 JDK 21
  （Microsoft OpenJDK 21.0.6.7）探针逐值对拍（bound=3/12/16/1000 等；2 的
  幂走位提取、其余走取模；nextFloat = next(24)/2^24），TS `SimRandom` 与
  Java 序列 bit-exact（`tests/sim/rng.test.ts`）。26.2 寿命公式（7 族）与
  campfire 出生初速的随机抽取序列均由 JDK 探针实测 golden 锁死
  （`tests/sim/lifecycle.test.ts`「26.2 逐类型寿命 golden」）。

**证据边界**：26.2 分区已覆盖所有"世界数据无关"的粒子类型（约 50 个，逐
构造器/逐 tick 字节码核对）；表内类型 1:1，表外类型（dust 系、世界相关系
等）不做。1.21.11 分区仍只有 end_rod（该版本其余类型未核对，不臆想跨版本
一致）。age-progress 选帧与后半程淡出是 `AnimatedParticle` 的**通用**行为
（对所有多帧类型生效，预览对所有 N>1 帧类型套用）；颜色插值仅 end_rod 有
证据，其他多帧类型不插值。
